<%= render 'refinery/admin/search_header', :url => refinery.feedbacks_admin_settings_path %>
<div class='pagination_container'>
  <% if @settings.any? %>
    <%= render 'settings' %>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
